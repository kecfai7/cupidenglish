# ✉️ Cupid English 이메일 발송 설정 가이드

안녕하세요! Cupid English 학습자 여러분이 연습한 큐피드 영어식 순차 학습 카드(주인공 ➔ 동작 ➔ 대상...)를 친구, 동료 혹은 스스로에게 이메일로 손쉽게 전송할 수 있도록 **3가지 이메일 발송 옵션**을 제공하고 있습니다.

노트북이나 새로운 PC에서 다른 사용자들이 쉽게 이메일 연동을 완료할 수 있도록, 각 방식별 상세 설정 단계를 안내합니다.

---

## ⚙️ 이메일 전송 방식 비교 및 선택
본인에게 맞는 방식을 선택하여 설정해 주세요.

| 전송 방식 | 난이도 | 장점 | 단점 / 제한사항 |
| :--- | :---: | :--- | :--- |
| **1. 간편 Gmail 편지쓰기 (강추)** | ⭐ (쉬움) | 설정이 필요 없고, **즉시** 안전하게 발송 가능 | Gmail 창이 켜지면 본문에 **Ctrl + V**를 직접 눌러야 함 |
| **2. Google Gmail API 자동 연동** | ⭐⭐⭐ (보통) | 버튼 **1번** 클릭으로 즉시 자동 발송됨 | 발급 토큰 유효기간이 **1시간**으로 만료 시 재발급 필요 |
| **3. EmailJS 백그라운드 연동** | ⭐⭐⭐⭐ (높음) | 토큰 만료 없이 **평생 자동 발송** 가능 | EmailJS 사이트 가입 및 템플릿 설정 필요 |

---

## 🚀 방식별 상세 설정 방법

---

### 1️⃣ [추천] 간편 Gmail 편지쓰기 창 열기 (설정 없음)
가장 빠르고 복잡한 API 연동 설정 없이 바로 쓸 수 있는 직관적인 방법입니다.

1. 학습 완료 후 **`이메일 보내기`** 모달을 엽니다.
2. 수신 이메일 주소를 입력하고 추가합니다.
3. 아래의 **`Gmail 편지쓰기 창 열기`** 버튼을 클릭합니다.
4. **자동 동작**:
   * 학습 내용(이미지, 큐피드 단계, 영어 문장, 원어민 추천 뉘앙스 등)이 예쁜 카드 형태로 **클립보드에 자동 복사**됩니다.
   * 새 창으로 구글 Gmail 편지쓰기 화면이 열리며, **수신인 주소**와 **이메일 제목**이 자동으로 채워집니다.
5. 열린 메일 본문 빈 곳을 클릭하고 **`Ctrl + V` (붙여넣기)**를 누릅니다.
6. 예쁘게 들어간 학습 카드를 확인하고 **`보내기`**를 누르면 끝!

---

### 2️⃣ Google Gmail API 1초 자동 직발송 연동 (Access Token 방식)
붙여넣기 과정 없이 버튼 클릭 한 번으로 상대방에게 즉시 메일을 쏘는 공식 API 방식입니다.

#### 💡 방법 A: Google OAuth Playground (가장 빠른 30초 발급)
개발자 설정 없이 구글에서 제공하는 테스트 도구로 토큰을 바로 얻는 방법입니다.

1. **[Google OAuth Playground ↗](https://developers.google.com/oauthplayground/)** 사이트에 접속합니다.
2. 왼쪽의 **Select API** 목록에서 아래로 스크롤하여 **`Gmail API v1`**을 찾습니다.
3. 해당 항목을 클릭하여 하위의 **`https://www.googleapis.com/auth/gmail.send`**에 체크합니다.
4. 아래의 파란색 **`Authorize APIs`** 버튼을 클릭합니다.
5. 연동할 본인의 구글 계정으로 로그인한 뒤, 권한을 **허용(선택)**해 줍니다.
6. 화면이 다시 Playground로 돌아오면, Step 2의 **`Exchange authorization code for tokens`** 버튼을 클릭합니다.
7. 오른쪽에 생성된 **`Access Token`** (값 예시: `ya29.a0A...`으로 시작하는 긴 문자열)을 복사합니다.
8. Cupid English 웹 앱의 이메일 설정 창에서 **`Gmail Access Token`** 입력란에 붙여넣기 한 뒤 저장합니다.
   * *⚠️ 주의: 구글 보안 표준 정책으로 인해 이 토큰은 발급 후 **1시간(60분) 동안만 유효**합니다. 1시간이 지나 메일 전송이 실패하면 토큰을 재발급받아야 합니다.*

#### 💡 방법 B: Google Cloud Console (공식 OAuth Client ID 연동)
한 번 연동해두면 간편하게 구글 로그인 버튼 클릭으로 토큰을 자동 갱신하는 방식입니다.

1. **[Google Cloud Console ↗](https://console.cloud.google.com/apis/credentials)**에 접속하여 프로젝트를 생성합니다.
2. **`API 및 서비스`** ➔ **`라이브러리`**에서 **Gmail API**를 검색하여 사용 설정(Enable)합니다.
3. **`사용자 인증 정보`** 메뉴 ➔ **`사용자 인증 정보 만들기`** ➔ **`OAuth 클라이언트 ID`**를 선택합니다.
4. 애플리케이션 유형을 **`웹 애플리케이션`**으로 선택합니다.
5. **승인된 자바스크립트 출처**에 본 프로그램 주소(`http://localhost:5173` 또는 배포된 사이트 주소)를 추가합니다.
   * *⚠️ 배포 주소나 포트 번호가 다르면 `origin_mismatch` 오류가 납니다. 이메일 창에 표시된 접속 주소를 그대로 복사해서 추가해 주세요.*
6. 생성된 **클라이언트 ID** (예: `xxxxxx.apps.googleusercontent.com`)를 복사합니다.
7. Cupid English 이메일 창의 **`Google OAuth Client ID`** 입력 칸에 붙여넣고 **`Google 로그인 연동`**을 눌러 인증을 완료합니다.

---

### 3️⃣ EmailJS 백그라운드 영구 동기화 설정 (영구 무료 자동 발송)
가입과 초기 설정이 필요하지만, 한 번 등록해두면 다른 사용자들도 토큰 만료 없이 평생 백그라운드 자동 전송을 이용할 수 있습니다.

1. **[EmailJS 공식 홈페이지 ↗](https://www.emailjs.com/)**에 회원가입(무료) 후 로그인합니다.
2. **`Email Services`** 메뉴에서 **`Add Service`**를 눌러 본인의 Gmail 또는 이메일을 연동합니다.
   * 이때 부여받은 **`Service ID`**를 메모합니다. (예: `service_xxxx`)
3. **`Email Templates`** 메뉴에서 **`Create New Template`**을 누릅니다.
   * 템플릿의 변수명을 아래와 같이 설정하여 저장합니다:
     * 수신자: `{{to_email}}`
     * 발신자: `{{from_name}}`
     * 메일 제목: `{{subject}}`
     * 메일 본문: `{{message}}` (HTML 형식 지원)
   * 템플릿 저장을 완료한 뒤 **`Template ID`**를 메모합니다. (예: `template_xxxx`)
4. **`Account`** (좌측 하단 혹은 프로필) ➔ **`API Keys`**로 이동하여 **`Public Key`**를 메모합니다. (예: 긴 난수 키)
5. Cupid English 이메일 모달에서 **`기타 보조 전송 옵션`** ➔ **`EmailJS 연동 설정 열기`**를 누르고, 메모한 3가지 값(**Service ID, Template ID, Public Key**)을 정확히 입력한 뒤 **`EmailJS 키 저장`**을 누릅니다.
6. 이제 **`⚡ EmailJS 자동 발송`** 버튼을 누르면 로그인 과정 없이 백그라운드에서 즉시 전송됩니다!

---

## 🛠️ 자주 발생하는 문제 및 해결 방법 (FAQ)

### Q. 메일 발송을 누르면 "Gmail API 전송 실패" 혹은 "토큰 만료" 오류가 납니다.
> **해결방법**: Google Access Token 방식은 보안을 위해 **1시간 동안만 사용 가능**합니다. 1시간이 지나면 자동으로 만료되므로, 메일을 다시 보낼 때 [방법 2-A]를 참고하여 토큰을 다시 발급받아 붙여넣어 주세요. 
> 이 과정이 번거롭다면 **`간편 Gmail 편지쓰기 창 열기 (Ctrl+V)`**를 사용하시는 것을 적극 권장합니다.

### Q. Google 로그인 연동 중 `origin_mismatch` 에러 창이 뜹니다.
> **해결방법**: 구글 개발자 콘솔(Google Cloud Console)의 OAuth 클라이언트 ID 설정에서 **승인된 자바스크립트 출처(Authorized JavaScript Origins)**에 현재 사용 중인 정확한 주소(포트 번호 포함, 예: `http://localhost:5173`)가 등록되어 있지 않아 보안상 거부된 것입니다. 
> 구글 콘솔로 다시 접속하여 현재 웹사이트 주소를 허용 리스트에 추가해 주세요.
